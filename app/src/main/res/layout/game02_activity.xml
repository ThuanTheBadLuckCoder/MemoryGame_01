<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:gravity="center"
    android:padding="16dp">

    <!-- ImageView for the first card -->
    <!-- ImageView for the first card -->
    <ImageView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:src="@drawable/card_1"
        android:contentDescription="@string/card_description"
        custom:randomImage="@drawable/card_1" />

    <!-- ImageView for the second card -->
    <ImageView
        android:id="@+id/cardImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/card_placeholder"
        android:contentDescription="@string/card_description"
        custom:randomImage="@drawable/card_1" />

    <!-- ImageView for the third card -->
    <ImageView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:src="@drawable/card_9"
        android:contentDescription="@string/card_description"
        custom:randomImage="@drawable/card_1" />

</LinearLayout>
